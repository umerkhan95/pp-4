*: việc cần làm
-: việc đã làm

Note 23/06/2025:
- Đã tích hợp MQTT cho board (dùng broker hivemqtt).
- Đã đảo lại logic cho 2 động cơ (trước: Relay1 - Sprinkler, Motor - Filter; hiện tại: Motor - Sprinkler, Relay1 - Filter).
- BOARD HIỆN TẠI ĐANG CẦN ANTEN ĐỂ THỰC HIỆN CÁC TÁC VỤ LIÊN QUAN WIFI.


Note 24/06/2025:
* Bỏ sensor PT100
* Bỏ HTTP POST/GET
* thêm chức năng scan wifi và gửi wifi list qua BLE
* thêm chức năng nhận lệnh qua BLE để kết nối hoặc re-scan wifi
(Các tác vụ BLE thì sẽ cấu hình sau, giờ chỉ đơn giản là nhận lệnh có thể qua serial)
* Check và list các trường hợp có thể khiến thời gian trong board bị sai lệch (hiện tại đang dùng thời gian từ server NTP và 1 hàm ngắt timer để cập nhật liên tục khi có wifi)

- Đã bỏ sensor PT100 (trong code)
- Đã bỏ HTTP POST/GET
- Vẽ lưu đồ hoạt động cho toàn hệ thống và cho task auto
- Thêm task check connect với server (wifi + MQTT)