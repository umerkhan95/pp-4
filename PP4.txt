*: việc cần làm
-: việc đã làm

============================Note 23/06/2025============================
- Đã tích hợp MQTT cho board (dùng broker hivemqtt).
- Đã đảo lại logic cho 2 động cơ (trước: Relay1 - Sprinkler, Motor - Filter; hiện tại: Motor - Sprinkler, Relay1 - Filter).
- BOARD HIỆN TẠI ĐANG CẦN ANTEN ĐỂ THỰC HIỆN CÁC TÁC VỤ LIÊN QUAN WIFI.

=======================================================================


============================Note 24/06/2025============================
* Bỏ sensor PT100
* Bỏ HTTP POST/GET
* thêm chức năng scan wifi và gửi wifi list qua BLE
* thêm chức năng nhận lệnh qua BLE để kết nối hoặc re-scan wifi
(Các tác vụ BLE thì sẽ cấu hình sau, giờ chỉ đơn giản là nhận lệnh có thể qua serial)
* Check và list các trường hợp có thể khiến thời gian trong board bị sai lệch (hiện tại đang dùng thời gian từ server NTP và 1 hàm ngắt timer để cập nhật liên tục khi có wifi)

- Đã bỏ sensor PT100 (trong code)
- Đã bỏ HTTP POST/GET
- Vẽ lưu đồ hoạt động cho toàn hệ thống và cho task auto
- Thêm task check connect với server (wifi + MQTT)
=======================================================================

============================Note 25/06/2025============================
* Chỉnh sửa API của các hàm liên quan timer phù hợp với bản esp 3.0.7
* Chỉnh sửa lại document (thay POST/GET thành MQTT) và push code lên git (ver mới nhất ổn định)
* thêm chức năng scan wifi và gửi wifi list qua BLE

- Đã done 2 ý đầu
- Đã viết xong hàm scan wifi, đã scan được wifi nhưng còn 1 lỗi nhỏ
=======================================================================

============================Note 26/06/2025============================
* Thêm task Manual để điều khiển động cơ với lệnh nhận từ App (Hiện tại sẽ test bằng lệnh gửi qua MQTT)
* Chạy default 1 lần cho mỗi động cơ, bắt đầu 5p sau khi khởi động. Bơm sẽ 
{
3 case:
	- Mất điện: không hoạt động
	- Có điện nhưng không có wifi: không wifi thì không get thời gian từ server nên không đồng bộ được thời gian => giải pháp: bắt đầu count từ 00:00:00, chạy hết period thì reset bộ đếm và lặp lại
	- Có điện và wifi: hoạt động bình thường
}

- Đã scan được wifi và có wifi list.
- Đã thêm task Manual và điều khiển được các động cơ ON/OFF
=======================================================================

============================Note 27/06/2025============================
* Clean code để flexible trong việc sửa hoặc phát triển thêm
* không wifi thì không get thời gian từ server nên không đồng bộ được thời gian => giải pháp: bắt đầu count từ 00:00:00, chạy hết period thì reset bộ đếm và lặp lại
=======================================================================

============================Note 28/06/2025============================
- Đã clean và chia ra thành các module để tiện cho việc kế thừa và phát triển thêm
=======================================================================

============================Note 30/06/2025============================
- Trong trường hợp không có app + wifi, sẽ chạy auto default với Sprinkler 1 lần/ngày và 4 min/lần, Filter 1 lần/ngày và 4 hour/ngày
- Đã fix lỗi logic chương trình bị treo khi chưa config wifi (nhưng có bug mới là không kết nối được wifi)
- Fix các lỗi logic trong hoạt động
=======================================================================

============================Note 01/07/2025============================
- Chuyển sang dùng timer thay cho NTP server (chưa hoàn thành)
- Support team test ở China test firmware.

Có 2 timer, sprinklerTimer (đếm mỗi khi đến period thì set cờ và reset) và counterTimer (đếm mỗi giây đến khi có lệnh dừng)
StartTimer khi nào?
=> Trong setup và chạy mặc định 1 lần/ngày, bắt đầu count từ 00:00:00
=> Khi user setting và gửi xuống board:
  - Lấy thời gian hiện tại từ App (Thứ ngày tháng năm giờ phút giây). Kiểm tra giờ hiện tại với start_time:
   + Chưa qua start_time: tính toán xem còn bao lâu nữa là đến start_time và cho (counterTimer) đếm đến lúc đó để bắt đầu SprinklerTimer, đến start_time rồi thì cập nhật lại thời gian hiện tại và reset SprinklerTimer và dừng counterTimer. (quy sang phút) 
   + Đã qua start_time: (cho các động cơ chạy), tính toán xem đã qua bao lâu rồi để tính xem còn bao lâu nữa là đến cycle tiếp theo (quy sang phút)
=======================================================================

============================Note 02/07/2025============================
* Check lại hoạt động của 2 pump khi chạy default
